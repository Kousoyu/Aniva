# Aniva v0.0.0.1 — 架构设计

---

## 总览

v0.0.0.1 的核心是一个 **最小神经生命核**。

它由以下部分组成：

```
┌─────────────────────────────────────────────┐
│                  环境（Environment）          │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │          生命核（Life Core）             │  │
│  │                                        │  │
│  │   ┌──┐    ┌──┐    ┌──┐    ┌──┐        │  │
│  │   │单│───→│单│───→│单│←──→│单│        │  │
│  │   │元│←──→│元│    │元│───→│元│        │  │
│  │   └──┘    └──┘    └──┘    └──┘        │  │
│  │     ↕       ↕       ↕       ↕          │  │
│  │   ┌──┐    ┌──┐    ┌──┐    ┌──┐        │  │
│  │   │单│───→│单│←──→│单│    │单│        │  │
│  │   │元│    │元│───→│元│←──→│元│        │  │
│  │   └──┘    └──┘    └──┘    └──┘        │  │
│  │                                        │  │
│  │   200~500 个活性单元，互相连接          │  │
│  │   连接强度会随历史变化                  │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  环境提供：刺激源（强/弱/无）               │
│  环境接收：生命核的整体状态                  │
└─────────────────────────────────────────────┘
```

---

## 核心模块

### 1. 活性单元（Unit）

每个单元是生命核里最小的"活东西"。

**每个单元有这些属性：**

| 属性 | 说明 |
|---|---|
| 活性（activation） | 当前有多活跃，会持续变化 |
| 能量（energy） | 活动消耗能量，能量会慢慢恢复 |
| 阈值（threshold） | 活性超过这个值才会向外传播影响 |
| 历史痕迹（trace） | 过去活动留下的累积印记 |

**单元的行为不是写死的，而是由这些规则自然产生的：**

- 活性受到邻居和连接传来的影响
- 活性受到内部噪声/扰动的影响
- 活动消耗能量
- 能量低时活性减弱
- 能量会自动慢慢恢复
- 每次活跃都会在历史痕迹上留一笔

---

### 2. 连接（Connection）

单元之间通过连接互相影响。

**连接的属性：**

| 属性 | 说明 |
|---|---|
| 权重（weight） | 影响有多强，正数=兴奋，负数=抑制 |
| 方向 | 谁影响谁（可以是双向的） |

**连接的变化机制：**

- **用则强**：两个单元经常一起活跃 → 连接变强
- **不用则弱**：长时间不同时活跃 → 连接慢慢变弱
- 这就是"历史痕迹"的一部分
- 这也是未来"记忆"和"习惯"的种子
- 以后版本可以加：新连接生长、旧连接消亡

**连接的类型：**

- 近距离连接：影响空间上相邻的单元
- 远距离连接：不一定相邻，但通过"连线"传递影响
- 两者共存，就像真实大脑一样

---

### 3. 活性流动（Dynamics）

这是生命核"活起来"的核心机制。

**三种影响方式共存：**

**（一）突触传递（像传话）**
- 单元 A 活跃 → 通过连接把信号传给单元 B
- B 收到后根据自身状态决定怎么反应
- 精确的点对点信号
- 是思维、记忆、学习的基础

**（二）局部场效应（像引力）**
- 一个单元活跃时，空间上附近的单元也会被微微影响
- 不需要连接，靠空间距离产生影响
- 近的强，远的弱
- 让空间相近的单元更容易同步或互相干扰
- 是涌现模式的基础

**（三）弥散调节（像化学扩散）**
- 某些活动会释放"弥散信号"
- 不是点对点，而是像雾一样扩散到一片区域
- 影响区域内所有单元的整体状态
- 对应真实大脑的神经调质（多巴胺、血清素等）
- 改变整个区域的"基调"（更兴奋 or 更安静）
- 是未来"情绪"和"状态切换"的种子

**三种合在一起的效果：**
- 突触传递：精确通信 → 思维
- 局部场：空间同步 → 涌现
- 弥散调节：全局状态 → 情绪

**活性的基本流动：**

1. 每个单元的活性会影响它连接的其他单元（突触传递）
2. 同时微弱影响空间上的邻居（局部场）
3. 高活跃区域会释放弥散信号影响周围（弥散调节）
4. 有兴奋性影响（让对方更活跃）
5. 有抑制性影响（让对方更安静）
6. 兴奋和抑制的竞争 → 自然产生波动和节律

**为什么不会停：**

- 内部有自发活性（不需要外部输入也有微弱波动）
- 噪声/扰动持续存在
- 能量恢复机制让安静的单元有机会重新活跃
- 兴奋-抑制竞争让系统永远不完全平衡

**为什么不会乱掉：**

- 能量有限，不可能所有单元同时全力活跃
- 抑制性连接会压制过度兴奋
- 这就是"局部抗熵"——不是全部有序，也不是全部混乱

---

### 4. 历史痕迹（Plasticity & Trace）

**两种层面的历史：**

**单元层面：**
- 每个单元有一个累积痕迹值
- 每次活跃都会加深痕迹
- 痕迹影响未来行为（比如痕迹深的单元更容易被激活）
- 痕迹会慢慢衰减，但不会完全消失

**连接层面：**
- 连接权重会根据两端单元的共同活动历史变化
- 用则强，不用则弱
- 这是最原始的"学习"和"记忆"

---

### 5. 扰动（Noise & Perturbation）

**扰动来源：**

- 每个单元每时每刻都有微小随机波动
- 波动幅度不大，但长期累积会造成轨迹分叉
- 这是"不确定性"和"蝴蝶效应"的来源

**为什么需要扰动：**

- 没有扰动 → 系统会走向固定稳态 → 死了
- 有扰动 → 系统永远有微小偏差 → 永远有新可能

---

### 6. 能量（Energy）

**极简版能量系统：**

- 每个单元有一个能量值
- 活动消耗能量
- 能量每步自动恢复一点（像呼吸/代谢）
- 能量低 → 活动弱
- 能量高 → 更容易活跃

**能量带来的自然现象：**

- 波动节律：活跃 → 消耗 → 安静 → 恢复 → 再活跃
- 局部疲劳：某个区域活动太多会暂时安静
- 自然分布：不会所有地方同时一样亮

---

### 7. 环境（Environment）

**第一版极简环境：**

- 环境是一个围绕生命核的简单空间
- 可以在某些位置放置"刺激源"
- 刺激源有属性：强度、位置
- 刺激会影响附近的单元（增加或减少它们的活性输入）
- 刺激可以被添加、移除、改变

**环境不是主角：**

第一版环境只是为了验证"外界能影响内部"这一条。
复杂世界留给以后。

---

## 时间模型

**连续时间，小步长模拟。**

- 系统以很小的时间步不断推进
- 每一步，所有单元根据当前状态和输入更新
- 不同单元可以有不同的时间常数（有的反应快，有的慢）
- 看起来像持续流动，不是一格一格跳的

---

## 初始化策略（先天种子）

**第一版的"出生"：**

- 随机生成 200~500 个单元
- 随机分配初始位置
- 随机建立一批初始连接（稀疏，不是全连接）
- 连接权重有正有负（兴奋 + 抑制）
- 每个单元的初始参数有微小差异

**为什么用随机：**

- 先天差异来自随机种子
- 不同种子 → 不同初始结构 → 不同发展轨迹
- 这就是"先天种子"的第一版实现

---

## 模块关系图

```
Environment
    │
    ▼ (刺激输入)
┌────────────────────────────────┐
│         Life Core              │
│                                │
│  Units ←──→ Connections        │
│    │            │              │
│    ▼            ▼              │
│  Dynamics    Plasticity        │
│    │            │              │
│    ▼            ▼              │
│  Energy      Trace             │
│    │                           │
│    ▼                           │
│  Noise                         │
└────────────────────────────────┘
    │
    ▼ (状态输出 → 可视化)
Observer / Visualizer
```

---

## 代码模块规划

```
aniva/
├── core/
│   ├── unit.py          # 活性单元
│   ├── connection.py    # 连接
│   ├── dynamics.py      # 活性流动 + 更新规则
│   ├── plasticity.py    # 连接变化 + 历史痕迹
│   ├── energy.py        # 能量系统
│   └── noise.py         # 扰动
├── environment/
│   └── environment.py   # 极简环境
├── life_core.py         # 生命核（组装以上模块）
├── observer.py          # 状态观测 + 数据记录
├── visualizer.py        # 实时可视化
├── experiments/
│   ├── exp1_free_run.py        # 实验1：零输入持续活动
│   ├── exp2_stimulus.py        # 实验2：环境刺激响应
│   ├── exp3_divergence.py      # 实验3：轨迹分叉
│   └── exp4_emergence.py       # 实验4：局部组织涌现
├── config.py            # 参数配置
└── main.py              # 启动入口
```

---

## 第一版参数参考

| 参数 | 建议值 | 说明 |
|---|---|---|
| 单元数量 | 200~500 | 可配置 |
| 连接密度 | ~5% | 稀疏连接，每个单元平均连几十个 |
| 兴奋/抑制比 | ~80%/20% | 接近真实大脑比例 |
| 时间步长 | 0.1~1.0 ms（模拟） | 小步长模拟连续时间 |
| 噪声强度 | 低 | 够造成分叉，不至于压过信号 |
| 能量恢复速率 | 慢 | 让波动节律自然出现 |
| 连接变化速率 | 很慢 | 历史是慢慢积累的 |

---

*架构版本：v0.0.0.1*
*日期：2026-04-25*
